# Example environment variables for notes_backend
# Copy this file to .env and adjust values as needed.

# Application
APP_NAME=Simple Notes API
ENVIRONMENT=development

# Optional JSON file persistence for notes repository.
# Use a file URL. Examples:
#   file://notes.json              -> relative path in container
#   file:///data/notes.json        -> absolute path
# If not set, the app will run purely in-memory.
NOTES_DB_URL=file://notes.json

# CORS configuration (comma-separated)
CORS_ALLOW_ORIGINS=*
CORS_ALLOW_METHODS=*
CORS_ALLOW_HEADERS=*
CORS_ALLOW_CREDENTIALS=true
